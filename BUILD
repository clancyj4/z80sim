Make sure you have the relevant GTK development libraries installed.

(libgtk-3-dev on Ubuntu).

Also the usual C building programs (gcc, make etc)

cd to the cloned directory and issue the command 'make'.

That's it (I hope).

